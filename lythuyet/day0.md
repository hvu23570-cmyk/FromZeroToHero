1. Cấu tạo của một Component
- Một Component thường bao gồm 3 phần chính gắn liền với nhau:

+ Giao diện (HTML): Cấu trúc hiển thị (nút bấm, ô nhập liệu, hình ảnh...).

+ Thẩm mỹ (CSS): Cách phần đó trông như thế nào (màu sắc, kích thước).

+ Logic (JavaScript): Cách nó hoạt động (khi nhấn vào nút thì chuyện gì xảy ra).

1️⃣ JavaScript 

- const a = 1;        // biến
- if (a > 0) {}       // điều kiện
- function f() {}     // hàm

2️⃣ static-ui = form + validate
Form login
Check:
- email có @
- password ≥ 6
- Sai → alert

3️⃣ FE (React)
- Component = 1 khối giao diện
- useState = biến để lưu dữ liệu tạm
- useEffect = chạy khi trang load


- “hiểu FE chưa?”
-" đang làm quen, hiện hiểu luồng component – state – gọi API cơ bản .”

4️⃣ BE – CHỈ BIẾT NÓ TỒN TẠI
- Bạn không cần viết BE.
- Chỉ cần hiểu:
+ FE gửi request
+ BE trả response
+ JSON là dữ liệu

5️⃣ Biết giải thích LUỒNG 

- Người dùng → giao diện → kiểm tra dữ liệu → gửi server → nhận kết quả → hiển thị

